- name: install core packages
  hosts: "host1"
  become: yes
  tasks:
    - name: install core packages
      apt:
        name:
          - prometheus-node-exporter
          - prometheus-cadvisor
        update_cache: yes